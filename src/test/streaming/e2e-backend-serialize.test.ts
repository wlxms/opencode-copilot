// Integration test — temporarily stubbed during SSP/SSS/Bridge architecture refactoring.
// These tests used the old bridge API (processEvent, setStream, bridgeEventsToStream)
// which has been replaced by push/update on SSS. Rewrite needed.

describe('e2e-backend-serialize (stubbed)', () => {
  it.skip('TODO: rewrite for new SSS push/update API', () => {});
});